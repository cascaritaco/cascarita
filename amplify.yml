version: 1
applications:
  - name: frontend
    phases:
      preBuild:
        commands:
          - npm ci --cache .npm --prefer-offline
      build:
        commands:
          - npm run build
    artifacts:
      baseDirectory: dist
      files:
        - "**/*"
    cache:
      paths:
        - .npm/**/*

  - name: backend
    phases:
      preBuild:
        commands:
          - npm ci --cache .npm --prefer-offline
      build:
        commands:
          - echo "No build step for backend"
      start:
        commands:
          - npm start
    cache:
      paths:
        - .npm/**/*
